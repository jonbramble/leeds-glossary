<h1>Listing subtopics</h1>

<table>
  <tr>
    <th>User</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<p><b>Topic:</b><%= @topic.name %></p>

<% @subtopics.each do |subtopic| %>
  <tr>
    <td><%= subtopic.user.name %></td>
    <td><%= subtopic.name %></td>
    <td><%= link_to 'Show', topic_subtopic_path(@topic,subtopic) %></td>
    <td><%= link_to 'Edit', edit_topic_subtopic_path(@topic,subtopic) %></td>
    <td><%= link_to 'Destroy', topic_subtopic_path(@topic,subtopic), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subtopic', new_topic_subtopic_path %>
